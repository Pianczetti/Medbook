{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
  <div class="row justify-content-end mb-3">
    <div class="col-auto">
      <a href="{{ path('admin_medbook_booking_booking_create') }}" class="btn btn-primary">
        <i class="material-icons">add_circle_outline</i>
        {{ 'Add new booking'|trans({}, 'Modules.Medbookbooking.Admin') }}
      </a>
    </div>
  </div>

  <div class="row">
    <div class="col">
      {% embed '@PrestaShop/Admin/Common/Grid/grid_panel.html.twig' with {'grid': bookingGrid} %}{% endembed %}
    </div>
  </div>
{% endblock %}
